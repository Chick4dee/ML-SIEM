# ML-SIEM — финальный план проекта

> 🔴 **БЭКАП:** этот файл самодостаточен (план + обоснования + setup + как продолжить).
> Перед переустановкой Windows сохрани его ВНЕ этого компьютера (GitHub + облако/USB).
> См. раздел 13 «Как продолжить после переустановки».

> Учебный, но «production-grade» гибридный SIEM/SOAR с детекцией на самостоятельно
> обученных нейросетях. Цель — портфолио: охватить максимум маркетируемых технологий
> (ML + Backend + Frontend + Data + DevOps + Security).

Статус: **фаза планирования**. Код и Figma не начинаем до явной команды «начинай».
Дата фиксации плана: 2026-06-07.

---

## 1. Видение

Система принимает сетевые события (flow + payload), прогоняет их через **ансамбль
нейросетей-экспертов** (Mixture-of-Experts), мета-слой выдаёт вердикт
`(класс атаки, severity, confidence)`, а детерминированный **SOAR-движок** предлагает
реакцию, которую аналитик подтверждает через **SOC-консоль (React UI)**.

Ключевой принцип разделения:
- **Нейросети** отвечают на «*что* происходит» (вероятностно).
- **SOAR (правила)** отвечает на «*что делать*» (детерминированно, объяснимо, обратимо).

---

## 2. Архитектура (все слои)

```
        ┌──────────────┐
        │  INGESTION   │  Kafka (стрим) + replay датасетов + (опц.) live capture
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │  FEATURE /    │  polars, нормализация, кодирование,
        │  WINDOWING    │  нарезка окон-последовательностей (по src IP + время)
        └──────┬───────┘
               ▼
   ┌───────────────────────── DETECTION (PyTorch) ─────────────────────────┐
   │  Эксперт 1: Flow CNN+LSTM   Эксперт 2: Temporal CNN+LSTM               │
   │  Эксперт 3: Payload char-CNN/Transformer   Эксперт 4: CNN-LSTM AE      │
   │                         ▼                                              │
   │              МЕТА-СЛОЙ (stacking + open-set + severity)                │
   └───────────────────────────────┬──────────────────────────────────────┘
                                    ▼
        ┌──────────────┐    вердикт {class, severity, confidence, src_ip}
        │ RESPONSE /    │  SOAR: триаж → playbook → предохранители →
        │ SOAR (rules)  │  СИМУЛЯЦИЯ действия → ожидание подтверждения
        │ + MITRE ATT&CK│
        └──────┬────────┘
               ▼
        ┌──────────────┐   PostgreSQL (инциденты/аудит) + OpenSearch (логи/поиск)
        │  STORAGE      │
        └──────┬────────┘
               ▼
        ┌──────────────┐   FastAPI (REST + WebSocket) → React UI (8 экранов)
        │  API + UI     │   Approve/Reject действий аналитиком
        └───────────────┘

  СКВОЗНОЕ: MLflow (эксперименты/реестр), ONNX (инференс),
            Prometheus+Grafana (observability), Docker-compose, GitHub Actions CI/CD
```

---

## 3. Данные — ГИБРИДНАЯ стратегия + ДВУХЭТАПНОЕ обучение

**Двухэтапная схема (transfer learning / domain adaptation) — ключ к консистентности:**

```
Этап 1 — PRETRAIN на публичных датасетах (фичи через nfstream из PCAP)
         → широкие знания об атаках: объём, разнообразие, бенчмарк
Этап 2 — FINE-TUNE / калибровка на данных полигона (тот же nfstream, та же среда)
         → адаптация к нашей сети; «норма» AE и пороги — отсюда
```

Это снимает domain shift (#6), фиксирует единый экстрактор (#1) и источник «нормы» (#4).
Подробности — в разделе «Консистентность train/serve».

Публичные и самосгенерированные данные играют **разные роли** (не дублируются):

| Источник | Роль | Что именно |
|---|---|---|
| **Публичные датасеты** | **PRETRAIN** сетевых моделей (объём, разнообразие, бенчмарк); фичи пересчитаны nfstream из PCAP | Эксперты 1–3 |
| **Свой лаб-датасет** (автономный полигон) | (1) **живое демо** = сам поток; (2) **FINE-TUNE/калибровка** под среду; (3) **«норма» автоэнкодера** (Эксперт 4); (4) **обучение HIDS** (Эксперт 5, Sysmon-first); (5) end-to-end валидация; (6) **held-out zero-day** | runtime + дообучение + Эксперты 4,5 + тесты |

> Вывод: публичные = претрейн (широта), полигон = дообучение + «норма» + калибровка +
> валидация (точность под среду). Свой датасет НЕ замена публичным, а вторая половина схемы.

### Источники по экспертам

| Эксперт | Датасет | Что покрывает |
|---|---|---|
| 1. Flow | **UNSW-NB15** | широкий спектр: DoS, Exploits, Recon, Fuzzers, Backdoors, Worms, Shellcode, Generic |
| 2. Temporal | **CTU-13** | реальные ботнеты, beaconing, временные паттерны (LSTM раскрывается) |
| 3. Payload | **CSIC 2010 HTTP** (претрейн) | SQLi, XSS, web brute force (контент пакета) |
| 4. Anomaly | **«норма» из ПОЛИГОНА (GHOSTS)** — НЕ публичная | автоэнкодер, zero-day/unknown |
| 5. Host (HIDS) | **Sysmon-first**: свои Sysmon-логи (Caldera/Atomic Red Team + GHOSTS); OpTC — опц. stretch | действия на хосте: команды, lateral movement, persistence (раскрывает Caldera) |

> ⚠️ «Норма» для AE (#4) и хост-данные (#5) берём из **полигона**, а не из публичных —
> «нормальность» это свойство КОНКРЕТНОЙ среды (иначе лавина ложных срабатываний).
> OpTC (eCAR) отвергнут как основной для HIDS из-за рассинхрона схемы с Sysmon — оставлен опционально.
> Диск ~2 ТБ → полные PCAP UNSW-NB15/CTU-13 качаем целиком для переэкстракции nfstream.

Риски данных: дисбаланс классов → focal/weighted loss, метрика **PR-AUC** (не accuracy).
Корректный временной сплит, чистка дубликатов, защита от ликеджа.

> ⚠️ **Train/serve consistency:** фичи из живой лаборатории считаем **тем же экстрактором**,
> что и при обучении (**nfstream** для flow), иначе модель «увидит» другие признаки, чем училась
> (train/serve skew — частая причина провала live-NIDS). По возможности обучаем на фичах,
> пересчитанных из PCAP, а не на «чужих» CSV.

---

## 4. Детекция — 5 экспертов + мета

- **Эксперт 1 — Flow CNN+LSTM:** `[B, N, F]` → TimeDistributed Conv1D → LSTM → softmax (мультикласс).
- **Эксперт 2 — Temporal CNN+LSTM:** окна потоков по времени, фокус на периодичности (beaconing) и медленных атаках.
- **Эксперт 3 — Payload char-CNN / мини-Transformer:** байты/строки запроса → класс web-атаки.
- **Эксперт 4 — CNN-LSTM Autoencoder:** учится на benign, reconstruction error > порог ⇒ аномалия.
  ⚠️ **Реалистичная роль (урок v1):** AE — НЕ универсальный детектор. Его зона = волюметрические/
  тайминговые аномалии (DDoS, DoS, scan) и их zero-day. Скрытные атаки (brute, bot, web) на flow
  неотличимы от нормы — их закрывают Эксперты 1/2 (supervised), 3 (payload), 5 (host). Не гнаться
  за высоким AUC по всем классам — AE это open-set страховка, не основной классификатор.
- **Эксперт 5 — Host CNN+LSTM (HIDS, Sysmon-first):** последовательности хостовых событий
  (**Sysmon** — train и serve в одной схеме, нулевой skew) → детект действий противника на машине
  (выполнение команд, persistence, lateral movement). Атаки для обучения — Caldera/Atomic Red Team,
  норма — GHOSTS. Добавляется **отдельной фазой после NIDS** — раскрывает Caldera на 100%.
- **Мета-слой (двухуровневый, согласовано):**
  1. **Калибровка** каждого эксперта (temperature scaling / Platt) — вероятности несравнимы без неё.
  2. **Routing/abstain** — какие эксперты применимы к событию (есть payload? есть host-лог?).
     «Эксперт промолчал» ≠ «норма»: мета явно учитывает применимость, а не считает молчание нормой.
  3. **Stacking-классификатор** над [калибр. вероятности применимых экспертов + AE anomaly score +
     confidences + флаги «кто промолчал»] → единый класс + калиброванная confidence.
     Stacking сам учится, кому доверять в каком классе → слабый-в-классе эксперт (напр. AE на
     скрытных) автоматически дисконтируется. Начинаем с rule/OR на MVP (фаза 4) → stacking (фаза 7).
  4. **Open-set (zero-day) — отдельная ветка:** `все supervised не уверены (max-prob < τ) И AE high
     ⇒ UNKNOWN`.
  + **корреляция сеть ↔ хост** (один инцидент из сетевого и хостового сигналов)
  + severity-движок (класс → критичность, дедуп, агрегация в инцидент).

  > ⚠️ **Ликедж мета (из 10.5):** stacking учим на выходах экспертов на **hold-out** данных, НЕ на их
  > train-выборках — иначе эксперты там «слишком уверены» и мета выучит неверные веса.
  > Единый словарь классов: эксперты со своими наборами маппятся в общую таксономию.

Окна: группировка по `src IP` + сортировка по времени, старт **N=16, stride=1**
(подберём эмпирически). Метка окна: класс последнего потока.

**Прогрев окна (warmup, #7):** на serve поток приходит инкрементально — для нового src IP
сначала < N потоков. Решение: **маскирование** (LSTM/attention игнорируют пустые позиции,
не зероы) + обучение на **маскированных неполных окнах** с верными метками + **гейтинг в SOAR**
(при контексте < 3 потоков — только alert/watch, без жёсткой авто-реакции). Ранняя видимость
без ложных действий. Одинаковая логика прогрева в train и serve — обязательна.

Обучение: PyTorch + CUDA 12.x + AMP на RTX 3060 Ti (8 ГБ). Инференс — на **GPU хоста**
(SIEM-наблюдатель на хосте, Вариант A); **ONNX-экспорт — опционально** (навык/портфолио + переносимость).

**Scope:** гибрид **NIDS + HIDS**, строим последовательно: сначала сетевые эксперты (1–4) до
рабочего end-to-end, затем хостовой эксперт (5) + корреляция.

---

## 4.5 Корреляция сеть↔хост и сборка инцидентов

Сетевые вердикты (ключ — IP) и хостовые (ключ — имя хоста) нужно сшить в **один инцидент**.

- **Asset-map `IP↔host`** — табличка соответствия (как «контакты»: номер↔имя). В полигоне —
  **статический конфиг-файл** (2 строки); код — с «розеткой» под внешний инвентарь (AD/DHCP/CMDB) на будущее.
- **Синхронизация времени (NTP) обязательна** на всех ВМ — иначе сетевые и хостовые события не выровнять.
- **Корреляция — правилами** (не обучаемой моделью): группировка по `(актив, временно́е окно ±Δ)`
  + связывание по цепочке ATT&CK (recon → exploit → C2 → persistence) → один инцидент.
- **Corroboration:** совпадение сеть+хост на одном активе **повышает** severity/confidence;
  отсутствие одного сигнала (напр. лёг host-агент) **НЕ понижает** — net-only/host-only инциденты валидны.
- **Гранулярность one-to-many:** одна атака → много хостовых событий → агрегируем, не плодим.
- **Инцидент = stateful-объект** (open → updated → resolved) с дедупом и накоплением улик, в PostgreSQL.

---

## 4.7 Стриминг и управление состоянием (ingestion → окна → инференс)

Конвейер реального времени через Kafka. Особенности из-за потоковой природы:

- **Состояние окна на источник:** копилка (deque) последних N потоков на каждый IP.
  **Выселение простаивающих** (TTL/LRU) — иначе память растёт на тысячах IP.
- **Порядок:** Kafka **партиционируем по ключу = IP** (сеть) / **host** (хост) → весь трафик одного
  источника в одной партиции, порядок сохраняется. Опоздавшие — маленький буфер пересортировки.
- **Event-time:** окна режем по **времени самого потока**, не по времени прихода (иначе replay
  с собственными метками ломается) + watermark для опоздавших.
- **Доставка at-least-once** (не exactly-once); дубликаты гасятся дедупом на уровне инцидентов.
- **Восстановление:** состояние в памяти, после рестарта — повторный прогрев (приемлемо для учебного);
  Redis-backed state — stretch.
- **Backpressure:** Kafka буферизует, если инференс (GPU хоста) медленнее притока; следим за лагом
  консьюмера → Prometheus/Grafana.
- **Общий оконный модуль** для batch-обучения и live-стрима — гарантия контракта окон (см. 10.5).
- **Хост-поток (Sysmon)** — тот же паттерн, окна по хосту.

---

## 5. Reaction — SOAR (детерминированный, без нейросетей)

- **Режим:** СИМУЛЯЦИЯ (ничего реально не блокируется; действия пишутся в аудит и показываются в UI).
- **Подтверждение:** human-in-the-loop — аналитик жмёт Approve/Reject в очереди реагирования.
- **Tiers действий:** 0 лог/обогащение → 1 уведомление → 2 мягкий (rate-limit/watch) →
  3 жёсткий (блок IP, симуляция) → 4 сдерживание (изоляция, только вручную).
- **Playbooks по классам:** PortScan → watch; Brute force → блок по N попыткам (fail2ban-паттерн);
  DDoS → агрегация + rate-limit; Bot/C&C → блок назначения + изоляция; UNKNOWN → только алерт.
- **Предохранители:** whitelist (шлюз/DNS/админ-IP), TTL + авто-откат, cooldown/дедуп,
  полный аудит-лог, высокий порог confidence на tier 3.
- **MITRE ATT&CK:** маппинг классов атак на техники (T-коды) для UI-матрицы.
- **Каналы уведомлений:** UI (всегда) + **Email (SMTP)** + **Webhook** (универсальный: Slack/Discord/Teams/Telegram).

### Стык SOAR ↔ детекция (согласовано)
- **SOAR работает с ИНЦИДЕНТАМИ** (сшитыми/сдедупленными из узла 4.5), не с сырыми алертами —
  иначе на один DDoS тысяча блокировок.
- **Полная карта `класс → playbook` + обязательный DEFAULT** для неожиданных классов;
  **UNKNOWN/zero-day → только алерт** (без авто-действий — нет уверенности в классе).
- **Тир действия = f(severity, confidence)** + гейтинг: низкая уверенность (прогрев окна,
  много «промолчавших» экспертов) → только мягкие тиры (watch/alert).
- **Идемпотентность:** одно ожидающее действие на инцидент + cooldown/TTL, а не новое правило на
  каждое обновление инцидента.
- **Машина состояний инцидент↔действие:** proposed → approved/rejected → executed → rolled-back;
  Approve/Reject синхронно обновляет инцидент и аудит-лог.
- **Многостадийный инцидент:** playbook по самой высокой severity, действия могут складываться.
- **Петля обратной связи:** причины Reject **храним сразу** (кандидаты в FP); полное дообучение
  моделей по ним — stretch на потом.

---

## 5.5 Demo / Cyber Range (демо-стенд)

> ⚠️ Всё — **только в изолированной лабораторной сети** (host-only/internal, без интернета),
> против **своих** намеренно уязвимых целей. Авторизованное тестирование в песочнице.

**Топология (Вариант A) — ХОСТ-SIEM (наблюдатель) + 2 ВМ под Hyper-V (Win11 Pro), ~16 ГБ RAM:**

```
   ВМ-АТАКУЮЩИЙ                       ВМ-ЖЕРТВА (Windows, одноразовая)
   Kali + Caldera-сервер             ├─ уязвимые сервисы (Juice Shop/DVWA)
   nmap/hydra/hping3/sqlmap   ───►    ├─ Sysmon → HIDS
   ATT&CK-сценарии                    ├─ Caldera-agent + GHOSTS (benign-фон)
        │      host-only сеть              │
        └──────────────┬───────────────────┘
                       ▼  (port mirroring Hyper-V → vNIC хоста)
        ХОСТ — SIEM-НАБЛЮДАТЕЛЬ (GPU)
        захват (nfstream) + приём Sysmon → детект (GPU) → мета → корреляция
        → SOAR → FastAPI + React UI (Approve/Reject) + PostgreSQL
```

- **SIEM — чистый наблюдатель на хосте** (никого не атакует, сам не мишень). Атакующий и жертва —
  отдельные ВМ → полная развязка трёх ролей.
- **Жертва = отдельная Windows-ВМ, НЕ реальный хост!** Одноразовая, со снапшотами, в **изолированной
  host-only сети** без интернета/домашней сети → атаковать её безопасно. Реальную ОС хоста не трогаем.
- **Захват:** inter-VM трафик зеркалируется на vNIC хоста через **port mirroring Hyper-V**.
- **Гипервизор:** Hyper-V (нативен в Win11 Pro, дружит с WSL2/Docker, снапшоты для отката жертвы).
- **GPU доступен на хосте** → инференс на **GPU**. **ONNX — опционально** (навык/портфолио + переносимость),
  не вынужденная необходимость.
- **RAM (16 ГБ, впритык):** во время демо обучение выключено, стек лёгкий; ВМ-Kali ~2.5 ГБ,
  ВМ-жертва ~4 ГБ, хост-SIEM lean ~4 ГБ. Тяжёлое (OpenSearch/Grafana) — по профилю.
- **Генерация атак:** **MITRE Caldera** (автоматические ATT&CK-цепочки, beaconing → Эксперт 2/5,
  наполняет MITRE-матрицу) + сетевые тулзы для «шумных» атак (скан/DoS/brute/web).
- **3 режима демо:** (A) replay датасета — для CI/регресса; (B) Docker-лаб — воспроизводимо;
  (C) ВМ-стенд — реалистично. Управление ресурсами — через docker-compose **profiles**
  (`demo` / `full` / `train`), чтобы не поднимать весь зоопарк сразу.
- **Payload-видимость (#3):** MVP — цели по **HTTP** (Эксперт 3 видит содержимое). Реалистичный
  апгрейд — **nginx reverse-proxy с TLS-терминацией**: клиенты ходят по HTTPS, payload инспектируем
  расшифрованным на прокси (паттерн настоящего WAF). TLS чужой не взламываем.
- **Benign-фон:** **GHOSTS** (CMU SEI) — симулятор «живых пользователей» (NPC: браузинг, почта,
  файлы) для правдоподобной «нормы» (самый сложный пункт) + автоэнкодер.
- **Разметка live-данных:** ground-truth по расписанию атак (знаем, когда что запускали).
- **Автономная генерация («датасет как код»):** оркестратор в цикле — откат снапшота → захват
  (tcpdump + Sysmon) → проигрывание timeline (GHOSTS-фон + Caldera/сетевые атаки) → стоп →
  авто-разметка по таймлайну → nfstream-фичи + хост-логи → рандомизация → повтор. Работает
  **офлайн и unattended** (интернет нужен только для первичной установки инструментов).

---

## 6. UI — SOC-консоль (8 экранов)

Дизайн: **Figma** (макеты) → реализация **React + TypeScript + Tailwind/shadcn**,
real-time через WebSocket.

1. **Overview** — счётчики, severity-распределение, график во времени, топ src IP.
2. **Лента алертов** — поток инцидентов (время, src→dst, класс, severity, confidence).
3. **Карточка инцидента** — детали + какой эксперт сработал и его уверенность + обогащение (GeoIP/reputation).
4. **Очередь реагирования** — предложенные действия с Approve/Reject (human-in-the-loop).
5. **Аудит-лог** — что предложено/сделано, кто подтвердил, откаты.
6. **Модели/метрики** — per-class precision/recall, confusion matrix, ROC, состояние моделей.
7. **MITRE ATT&CK Matrix** — подсветка сработавших техник.
8. **Threat Hunting / Search** — поиск по логам (OpenSearch).

---

## 7. Технологический стек (всё в Core)

| Слой | Технологии |
|---|---|
| ML/DL | PyTorch, CNN+LSTM, Autoencoder, Mixture-of-Experts, focal loss |
| MLOps | MLflow (трекинг + реестр), ONNX (экспорт/инференс), **DVC** (версионирование данных/моделей) |
| Данные | polars/pandas, Parquet |
| Стриминг | Kafka |
| Бэкенд | FastAPI, REST, WebSocket |
| БД | PostgreSQL (инциденты/аудит), OpenSearch (логи/поиск) |
| Фронтенд | React, TypeScript, Tailwind/shadcn |
| Дизайн | Figma |
| Реакция | SOAR-движок, MITRE ATT&CK |
| Observability | Prometheus, Grafana |
| DevOps | Docker, docker-compose, GitHub Actions (CI/CD), minikube (мини-витрина K8s в конце) |
| Уведомления | Email (SMTP), Webhook (Slack/Discord/Teams) |
| Качество | pytest, ruff, типизация |
| Захват/фичи | tcpdump/tshark, **nfstream** (единый flow-экстрактор train+serve), Sysmon (HIDS) |
| Лаборатория | Hyper-V (2 ВМ), Kali, **MITRE Caldera**, **GHOSTS** (benign NPC), nmap/hydra/hping3/sqlmap, OWASP Juice Shop/DVWA |

Kubernetes — **minikube как мини-витрина в самом конце** (фаза 22), не основной режим запуска.

---

## 8. Структура репозитория (предварительная)

```
S:\TestWork\
  data/            raw/ processed/
  notebooks/       исследование, EDA
  src/
    ingestion/     kafka producer/consumer, replay
    features/      preprocessing, windowing
    models/        flow_cnn_lstm/ temporal/ payload/ autoencoder/ meta/
    detection/     инференс-оркестратор экспертов
    response/      soar engine, playbooks, mitre mapping
    api/           fastapi app, websocket, schemas
    storage/       postgres, opensearch адаптеры
  ui/              react + ts приложение
  mlops/           mlflow конфиги, экспорт ONNX
  infra/           docker-compose, prometheus, grafana, opensearch
  tests/           pytest
  .github/workflows/  CI/CD
  docs/            PLAN.md, архитектура, экспорт Figma
```

---

## 9. Дорожная карта (фазы, каждая = рабочий результат)

| # | Фаза | Ключевой результат |
|---|---|---|
| 0 | Каркас + окружение + проверка CUDA на 3060 Ti | репо, venv, GPU виден, CI скелет |
| 1 | Данные: **PCAP публичных → nfstream-переэкстракция + переразметка**, чистка, фичи, **windowing (маски)** | `data/processed/*.parquet` |
| 2 | Эксперт 1 (Flow CNN+LSTM) + baseline + MLflow | обученная модель + метрики |
| 3 | Эксперт 4 (CNN-LSTM Autoencoder) + подбор порога | детектор аномалий |
| 4 | Мета-слой (правила) + гибридный вердикт | единый детект-модуль (MVP-детекция) |
| 5 | Эксперт 2 (Temporal на CTU-13) | temporal-детект |
| 6 | Эксперт 3 (Payload на CSIC/PCAP) | payload-детект |
| 7 | Мета-слой v2 (stacking) + полная оценка | калиброванный ансамбль |
| 8 | SOAR-движок (симуляция, playbooks, MITRE, предохранители) | очередь действий |
| 9 | Storage: PostgreSQL + OpenSearch | персист инцидентов + поиск |
| 10 | API: FastAPI + WebSocket | живые алерты наружу |
| 11 | Стриминг: Kafka ingestion | event-driven приём |
| 12 | Figma-макеты UI (8 экранов) | дизайн согласован |
| 13 | React UI (8 экранов) + Approve/Reject | живая SOC-консоль |
| 14 | Observability: Prometheus + Grafana | дашборды системы/моделей |
| 15 | MLOps: ONNX-инференс, реестр моделей | оптимизированный serving |
| 16 | DevOps: docker-compose всё-в-одном, CI/CD, тесты, README | запуск одной командой |
| 17 | Лаборатория (Вариант A): хост-SIEM (наблюдатель) + 2 ВМ (Kali-атакующий, Windows-жертва), Hyper-V port mirroring, host-only | стенд для атак |
| 18 | Live-захват + **nfstream** (консистентные фичи train/serve) | детект на живом трафике |
| 19 | Автономная генерация: Caldera + GHOSTS-фон + авто-разметка по таймлайну («датасет как код») | свой лаб-датасет (сеть+хост) |
| 20 | **HIDS:** сбор Sysmon-логов + Эксперт 5 (Host CNN+LSTM) + корреляция сеть↔хост | гибрид NIDS+HIDS |
| 21 | Полная оценка на лаб-данных + демо-видео/GIF + финальный README | портфолио-готовность |
| 22 | minikube: деплой ключевых сервисов в K8s (опц. витрина) | +навык Kubernetes |

DVC подключаем на фазе 1 (версионируем данные/пайплайны с самого начала).
Email/Webhook-уведомления — на фазах 8/10 (SOAR + API).
**Двухэтапное обучение:** этап fine-tune/калибровки на лаб-данных идёт после генерации полигона
(фаза 19) — каждая модель сначала pretrain (фазы 2,5,6), затем дообучение под среду.
**Критерии выхода по консистентности** (раздел 10.5) проверяются на КАЖДОЙ фазе с моделями/данными.
Shadow-mode + drift-мониторинг — на фазе 14 (observability) и перед демо (фаза 19/21).

Порядок гибкий: фазы 0–4 дают первый работающий вертикальный срез
(данные → детект → вердикт), дальше наращиваем экспертов, реакцию, UI, инфраструктуру и демо-стенд.

---

## 10. Решено / осталось

**Решено:**
- HIDS: **Sysmon-first** (свои Sysmon-логи; Caldera/Atomic Red Team + GHOSTS). OpTC — опц. stretch.
- Обучение: **двухэтапное** (pretrain на публичных через nfstream → fine-tune на полигоне).
- Консистентность train/serve: единый экстрактор nfstream, «норма» AE из полигона, маскирование
  окон, паритет ONNX↔PyTorch, сплит по времени, контракты данных (см. раздел 10.5).
- Payload (#3): **HTTP на MVP → nginx reverse-proxy HTTPS** для реализма.
- Payload-эксперт: **CSIC 2010 HTTP**.
- Группировка окон: **по src IP** + сортировка по времени (старт).
- Уведомления: UI + **Email + Webhook**.
- **DVC** — добавляем (с фазы 1).
- **minikube** — мини-витрина в конце (фаза 22).
- Портфолио: публичный GitHub, README, диаграммы, демо-видео.
- Train/serve consistency (nfstream), разметка по расписанию, набор метрик — приняты.

**Осталось уточнить по ходу (не блокирует старт):**
- Числовые цели метрик — зададим после первого прогона (фаза 2).
- Параметры окна `N`/stride и гейт-порог контекста — подберём эмпирически (фаза 1–2).
- OpTC (если возьмём как stretch): какой subset + маппинг eCAR→каноническая схема.

---

## 10.5 Консистентность train/serve и сквозные риски (КРИТИЧНО)

Главный системный риск проекта: модели учатся на публичных данных со своим экстрактором и своей
«нормой», а на вход идут данные полигона из нашего пайплайна. Без явных «контрактов» между
обучением и инференсом возникает **train/serve skew**. Карта конфликтов и решений:

| # | Конфликт | Что расходится | Решение |
|---|---|---|---|
| 1 | Экстрактор фич (Э1,2) | UNSW=Argus/Bro, CTU=binetflow, serve=nfstream | Переэкстракция фич из **PCAP через nfstream** на всех этапах |
| 2 | Переразметка | nfstream нарежет потоки иначе → метки «съедут» | Маппинг меток по 5-tuple + временному окну на ground-truth |
| 3 | Payload + HTTPS (Э3) | CSIC=plaintext, live может быть TLS | MVP по HTTP; апгрейд — **nginx reverse-proxy TLS-термин.** |
| 4 | «Норма» AE (Э4) | benign публичный ≠ benign нашей сети | AE учим/калибруем на **benign полигона (GHOSTS)** |
| 5 | Схема хоста (Э5) | OpTC eCAR ≠ Sysmon | **Sysmon-first** (train=serve=Sysmon); OpTC опц. |
| 6 | Среда измерения | датасет=реальная сеть, полигон=Hyper-V (RTT/TTL/MTU) | **Двухэтапка: pretrain→fine-tune** на лаб-данных |
| 7 | Онлайн-окна (warmup) | train=полные окна, serve=неполные на старте | **Маскирование** + train на масках + гейтинг в SOAR |
| 8 | Препроцессинг | scaler/encoder фитнутся на train | Сохранять/переиспользовать fitted-артефакты; OOV-обработка; общий код train/serve |
| 9 | ONNX-паритет | PyTorch(GPU) → ONNX(CPU) числа расходятся | **Тест паритета** ONNX vs PyTorch (tol < 1e-4) до деплоя |
| 10 | Таксономия классов | у каждого эксперта свой набор | Единый словарь + маппинг; open-set/AE для незнакомого |
| 11 | Сдвиг приоров/порогов | баланс train ≠ live | Калибровка порогов на распределении, близком к serve |
| 12 | Ликедж окон | stride=1 → соседние окна почти равны | Сплит **по времени/сессии**, не случайный |

**Метод (применяем по всему проекту):**
1. **Контракты данных на стыках** — для каждого интерфейса фиксируем схему, определения/единицы
   признаков, диапазоны, обработку пропусков/OOV. Несовпадение контрактов = баг до кода.
2. **Критерии выхода фазы по консистентности** — фаза не закрыта, пока не пройдены проверки:
   тот же экстрактор/препроцессинг, что на serve; fitted-артефакты переиспользуются; сплит без
   ликеджа; паритет ONNX↔PyTorch; распределения train vs live сверены (drift-чек).
3. **Shadow-mode + мониторинг дрейфа** — перед боевым демо гоняем на лаб-трафике в теневом режиме,
   сравниваем распределения входных фич с обучающими (PSI/KS). Это и фича SIEM (data drift) → в Grafana.

---

## 10.6 Уроки предыдущего проекта (v1) — чтобы НЕ повторить ошибки

У пользователя есть v1: гибрид CNN+LSTM + Conv1D AE на CICIDS2017, та же RTX 3060 Ti, стенд 2 ВМ.
Итог v1: AE в одиночку AUC 0.72–0.85, порог 70-й перцентиль → **~30% ложных срабатываний**;
скрытные атаки (Bot/FTPBrute/SSHBrute/PortScan/Web) **структурно неотличимы от нормы на flow-фичах**.

**Ключевая ловушка:** смена датасета (CICIDS→UNSW/CTU) НЕ чинит flow-слепоту — у любых flow-фич
тот же потолок на скрытных атаках. Решение — не «лучше датасет», а **другие представления**
(payload-Э3 + host-Э5), которых в v1 не было.

**Что забираем из v1 (проверенный рецепт AE):**
- Conv1D AE (НЕ Dense/Flatten — убивает время; НЕ GlobalAvgPool — то же).
- Достаточно узкое горлышко (latent ~64) — широкое горлышко даёт реконструкцию атак.
- Скоринг: Z-score top-K (peak + sustained) + Mahalanobis в латенте (×2.0).
- Ансамбль T=15 + T=10 (max); Δ-производные (вход ×2 признака); EarlyStopping по `val_recon`.
- PyTorch (TF на Windows = ад с cuDNN/CUDA — подтверждено v1).

**Что НЕ повторяем (задокументированные тупики v1):**
Dense/Flatten · GlobalAvgPool · широкое bottleneck · Denoising VAE (мешает детекции) ·
MemAE+OCSVM (хуже + лишние FP) · KL без warmup · EarlyStopping по суммарному loss.

**Обязательные условия (иначе повтор v1):**
- «Норма» AE — **разнообразный benign из полигона (GHOSTS критичен)**, не узкий/синтетический.
- Порог калибруем под **целевой FPR (1–5%)**, не «70-й перцентиль = 30% FP».
- Эксперты 3 (payload) и 5 (host) **обязательны** — без них слепые зоны flow остаются.
- 8 ГБ VRAM: **batch ≤ 256** (512 → CUDA OOM), `gc.collect()` + `del` промежуточных тензоров.

---

## 11. Чек-лист окружения (Фаза 0, после переустановки Win11 Pro)

1. **Windows 11 Pro** → включить компоненты **Hyper-V** + **WSL2**
   (`wsl --install`; Hyper-V — через «Программы и компоненты» или
   `Enable-WindowsOptionalFeature -Online -FeatureName Microsoft-Hyper-V -All`).
2. **NVIDIA-драйвер** для RTX 3060 Ti (последний Game Ready/Studio).
   CUDA-runtime тащит сам PyTorch — отдельный CUDA Toolkit не обязателен.
3. **Python 3.11** (python.org или pyenv-win).
4. **Docker Desktop** (бэкенд WSL2).
5. **Git** + аккаунт **GitHub** (для публичного репо-портфолио).
6. **VS Code** (или другой IDE) + расширения Python/Pylance.
7. Проверка GPU после установки PyTorch:
   `python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"`
   → должно вернуть `True NVIDIA GeForce RTX 3060 Ti`.
8. Для лаборатории (позже, не на фазе 0): образ **Kali Linux** для Hyper-V,
   **MITRE Caldera**, **GHOSTS**, уязвимые цели (**OWASP Juice Shop / DVWA**).

> ⚠️ Скачать инструменты/образы/пакеты НАДО до изоляции полигона — потом он работает офлайн.

---

## 12. Журнал решений и обоснований (почему именно так)

Это «память» проекта — чтобы при возвращении не переоткрывать споры.

- **Почему ансамбль (Mixture-of-Experts), а не одна модель?** Разные атаки живут в разных
  представлениях: DoS/скан — в статистике потоков, боты — во временной периодичности,
  SQLi/XSS — в payload, хостовые действия — в логах ОС. Одна модель видит один срез.
  Плюс сильный дисбаланс и open-set (нужно уметь сказать «это новое»).
- **Почему CNN+LSTM?** CNN — локальные паттерны внутри потока; LSTM — временные зависимости
  между потоками (beaconing, медленный скан, нарастающий DoS). Требует **нарезки в окна**
  (группировка по src IP + сортировка по времени) — без этого LSTM нечего «помнить».
- **Почему гибрид публичные + свой датасет?** Публичные дают объём, разнообразие и
  сравнимость с бенчмарками; свой полигон даёт живое демо, проверку на реальном трафике
  (train/serve consistency), обучение HIDS и held-out zero-day. Это РАЗНЫЕ роли, не дубль.
- **Почему свой датасет всё-таки нужен (хотя «почти не используется» для обучения сети)?**
  Его стоимость зашита в полигон, который нужен для живого демо; а функции (демо, валидация,
  HIDS-обучение, zero-day) публичными не закрываются.
- **Почему CIC-IDS2017 отвергнут?** Баги CICFlowMeter, дубликаты, ликедж → нестабильное
  обучение и завышенные метрики.
- **Почему train/serve consistency критична?** Модель учится на фичах из CSV, а в лаборатории
  трафик превращается в фичи другим инструментом → признаки не совпадают → детект не работает.
  Решение: один экстрактор (**nfstream**) и для обучения (из PCAP), и для live.
- **Почему Response — НЕ нейросети?** Реакция должна быть предсказуемой, объяснимой,
  обратимой — это правила/политики (SOAR), а не вероятности.
- **Почему симуляция + подтверждение аналитиком?** Безопасно (не отрежем себе сеть),
  и human-in-the-loop требует UI → UI стал обязательным, а не украшением.
- **Почему ONNX + CPU-инференс в ВМ?** Проброс GPU в ВМ — боль; обучаем на хосте (GPU) →
  экспорт ONNX → лёгкий CPU-инференс в ВМ. Это ещё и честный production-паттерн.
- **Почему 2 ВМ и Hyper-V?** 16 ГБ RAM: полный стек + тяжёлые ВМ одновременно не влезут,
  поэтому обучение и демо разнесены во времени, демо гоняет лёгкий срез (docker profiles).
  Hyper-V нативен в Win11 Pro, дружит с WSL2/Docker (без конфликтов VirtualBox), есть снапшоты.
- **Почему Caldera + GHOSTS?** Caldera — автоматизация ATT&CK-атак (хостовая активность,
  beaconing, наполняет MITRE-матрицу); GHOSTS — правдоподобный benign-фон (самый сложный
  пункт для автоэнкодера). Объёмные сетевые атаки — сетевыми тулзами (nmap/hydra/hping/sqlmap).
- **Почему DARPA OpTC только подмножеством?** Полный корпус — сотни ГБ–ТБ, на 16 ГБ нереально.
- **Цель проекта — портфолио:** отсюда «всё в Core» и максимум маркетируемых технологий
  (ML + Backend + Frontend + Data + DevOps + Security), но строим инкрементально, чтобы на
  каждом шаге был демонстрируемый результат.
- **Почему двухэтапное обучение (pretrain→fine-tune)?** Публичные данные ≠ наша среда (другой
  экстрактор, другая «норма», другие тайминги). Претрейн даёт широту, дообучение на полигоне —
  точность под среду. Решает разом #1/#4/#6/#11 и звучит как «transfer learning для NIDS».
- **Почему «норма» AE из полигона, а не из публичного?** «Нормальность» — свойство конкретной
  сети; AE на чужом benign дал бы лавину ложных срабатываний на нашем трафике.
- **Почему Sysmon-first для HIDS (а не OpTC)?** OpTC (eCAR) ≠ serve-схема (Sysmon); маппинг —
  кропотлив и сам источник skew. Sysmon train=serve → нулевой рассинхрон; разнообразие даёт
  Caldera/Atomic Red Team. Это пересмотр прежнего «OpTC + свои» по итогам анализа консистентности.
- **Почему HTTP на MVP, потом reverse-proxy HTTPS?** Пассивно расшифровать TLS нельзя; payload в
  реальности берут в точке терминации (WAF/reverse-proxy). HTTP — чтобы быстро завести Э3, прокси —
  для реализма.
- **Почему маскирование окон, а не нули?** Наивный паддинг = модель видит «мусор» на старте каждого
  хоста → проблемы детекции; маска заставляет игнорировать пустые позиции, плюс гейтинг в SOAR не
  даёт срывать жёсткие действия при коротком контексте.

---

## 13. Как продолжить после переустановки

1. **Сохрани этот файл вне компьютера** (см. ниже) — он самодостаточен.
2. После переустановки выполни **чек-лист окружения (раздел 11)**.
3. Открой новую сессию с ассистентом, дай ему этот файл (`PLAN.md`) и скажи:
   *«Вот план проекта ML-SIEM, продолжаем. Начинаем с Фазы 0»* (или с нужной фазы).
   Файл содержит все решения и обоснования — контекст восстановится полностью.
4. Дальше идём по **дорожной карте (раздел 9)**, фаза за фазой.

### ⚠️ Куда сохранить, чтобы не потерять при сносе Windows
Переустановка сотрёт и `S:\TestWork`, и память ассистента. Поэтому скопируй этот файл
**минимум в два места** из списка:
- **GitHub** — создай репозиторий (можно приватный) и закоммить `PLAN.md` (заодно старт портфолио).
- **Облако** — Google Drive / OneDrive / Dropbox / Яндекс.Диск.
- **USB-флешка / внешний диск**.
- **Письмо самому себе** на почту с вложением.

Рекомендация: **GitHub + облако** — так план переживёт что угодно и сразу станет частью портфолио.
